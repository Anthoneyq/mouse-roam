diff --git a/lan-mouse-ipc/src/lib.rs b/lan-mouse-ipc/src/lib.rs
index 17e9799..7b46bdb 100644
--- a/lan-mouse-ipc/src/lib.rs
+++ b/lan-mouse-ipc/src/lib.rs
@@ -186,6 +186,8 @@ pub struct ClientState {
 
 #[derive(Debug, Clone, Serialize, Deserialize)]
 pub enum FrontendEvent {
+    ControlStarted(ClientHandle),
+    ControlReturned,
     /// a client was created
     Created(ClientHandle, ClientConfig, ClientState),
     /// no such client
@@ -227,6 +229,8 @@ pub enum FrontendEvent {
 
 #[derive(Debug, Eq, PartialEq, Clone, Serialize, Deserialize)]
 pub enum FrontendRequest {
+    ReleaseInput,
+    SetInputEnabled(bool),
     /// activate/deactivate client
     Activate(ClientHandle, bool),
     /// add a new client
@@ -292,6 +296,7 @@ pub enum SocketPathError {
 
 #[cfg(all(unix, not(target_os = "macos")))]
 pub fn default_socket_path() -> Result<PathBuf, SocketPathError> {
+    if let Ok(path) = env::var("MOUSE_ROAM_SOCKET") { return Ok(PathBuf::from(path)); }
     let xdg_runtime_dir =
         env::var("XDG_RUNTIME_DIR").map_err(SocketPathError::XdgRuntimeDirNotFound)?;
     Ok(Path::new(xdg_runtime_dir.as_str()).join(LAN_MOUSE_SOCKET_NAME))
@@ -299,6 +304,7 @@ pub fn default_socket_path() -> Result<PathBuf, SocketPathError> {
 
 #[cfg(all(unix, target_os = "macos"))]
 pub fn default_socket_path() -> Result<PathBuf, SocketPathError> {
+    if let Ok(path) = env::var("MOUSE_ROAM_SOCKET") { return Ok(PathBuf::from(path)); }
     let home = env::var("HOME").map_err(SocketPathError::HomeDirNotFound)?;
     Ok(Path::new(home.as_str())
         .join("Library")
diff --git a/lan-mouse-ipc/src/listen.rs b/lan-mouse-ipc/src/listen.rs
index 8e26089..93eab8a 100644
--- a/lan-mouse-ipc/src/listen.rs
+++ b/lan-mouse-ipc/src/listen.rs
@@ -71,7 +71,7 @@ impl AsyncFrontendListener {
         };
 
         #[cfg(windows)]
-        let listener = match TcpListener::bind("127.0.0.1:5252").await {
+        let listener = match TcpListener::bind(format!("127.0.0.1:{}", std::env::var("MOUSE_ROAM_IPC_PORT").unwrap_or_else(|_| "5252".into()))).await {
             Ok(ls) => ls,
             // some other lan-mouse instance has bound the socket in the meantime
             Err(e) if e.kind() == ErrorKind::AddrInUse => {
diff --git a/src/capture.rs b/src/capture.rs
index 8f739bd..c7bdf9d 100644
--- a/src/capture.rs
+++ b/src/capture.rs
@@ -24,6 +24,8 @@ pub(crate) struct Capture {
 }
 
 pub(crate) enum ICaptureEvent {
+    ControlStarted(u64),
+    ControlReturned,
     /// a client was entered
     CaptureBegin(CaptureHandle),
     /// capture disabled
@@ -283,6 +285,9 @@ impl CaptureTask {
                         // connection acknowlegded => set state to Sending
                         ProtoEvent::Ack(_) => {
                             log::info!("client {handle} acknowledged the connection!");
+                            if self.state != State::Sending && self.active_client == Some(handle) {
+                                let _ = self.event_tx.send(ICaptureEvent::ControlStarted(handle));
+                            }
                             self.state = State::Sending;
                         }
                         // client disconnected
@@ -368,7 +373,7 @@ impl CaptureTask {
         if let Err(e) = self.conn.send(event, handle).await {
             const DUR: Duration = Duration::from_millis(500);
             debounce!(PREV_LOG, DUR, log::warn!("releasing capture: {e}"));
-            capture.release().await?;
+            self.release_capture(capture).await?;
         }
         Ok(())
     }
@@ -416,7 +421,9 @@ impl CaptureTask {
                 log::warn!("failed to send Leave to client {handle}: {e}");
             }
         }
-        capture.release().await
+        let result = capture.release().await;
+        let _ = self.event_tx.send(ICaptureEvent::ControlReturned);
+        result
     }
 }
 
diff --git a/src/config.rs b/src/config.rs
index f876b6d..ab3278e 100644
--- a/src/config.rs
+++ b/src/config.rs
@@ -496,9 +496,7 @@ impl Config {
 
     /// set configured clients
     pub fn set_clients(&mut self, clients: Vec<ConfigClient>) {
-        if clients.is_empty() {
-            return;
-        }
+
         if self.config_toml.is_none() {
             self.config_toml = Some(Default::default());
         }
diff --git a/src/connect.rs b/src/connect.rs
index 417d913..cb02f2c 100644
--- a/src/connect.rs
+++ b/src/connect.rs
@@ -65,7 +65,17 @@ async fn connect(
     tokio::select! {
         _ = timeout => Err((addr, LanMouseConnectionError::Timeout)),
         result = DTLSConn::new(conn, config, true, None) => match result {
-            Ok(dtls_conn) => Ok((Arc::new(dtls_conn), addr)),
+            Ok(dtls_conn) => {
+                let expected = std::env::var("MOUSE_ROAM_PEER_PIN")
+                    .ok().and_then(|path| std::fs::read_to_string(path).ok());
+                let certs = dtls_conn.connection_state().await.peer_certificates;
+                let actual = certs.first().map(|cert| crate::crypto::generate_fingerprint(cert));
+                if expected.as_deref().map(str::trim) != actual.as_deref() || actual.is_none() {
+                    let _ = dtls_conn.close().await;
+                    return Err((addr, LanMouseConnectionError::Dtls(webrtc_dtls::Error::ErrVerifyDataMismatch)));
+                }
+                Ok((Arc::new(dtls_conn), addr))
+            },
             Err(e) => Err((addr, e.into())),
         }
     }
diff --git a/src/emulation.rs b/src/emulation.rs
index 923bf99..e1d315a 100644
--- a/src/emulation.rs
+++ b/src/emulation.rs
@@ -67,6 +67,7 @@ pub(crate) enum EmulationEvent {
 }
 
 enum EmulationRequest {
+    SetEnabled(bool),
     Reenable,
     Release(SocketAddr),
     ChangePort(u16),
@@ -101,6 +102,10 @@ impl Emulation {
             .expect("channel closed");
     }
 
+    pub(crate) fn set_enabled(&self, enabled: bool) {
+        let _ = self.request_tx.send(EmulationRequest::SetEnabled(enabled));
+    }
+
     pub(crate) fn reenable(&self) {
         self.request_tx
             .send(EmulationRequest::Reenable)
@@ -139,6 +144,7 @@ struct ListenTask {
 impl ListenTask {
     async fn run(mut self) {
         let mut interval = tokio::time::interval(Duration::from_secs(5));
+        let mut enabled = false;
         let mut last_response = HashMap::new();
         let mut rejected_connections = HashMap::new();
         loop {
@@ -146,6 +152,10 @@ impl ListenTask {
                 e = self.listener.next() => {match e {
                     Some(ListenEvent::Msg { event, addr }) => {
                         log::trace!("{event} <-<-<-<-<- {addr}");
+                        if !enabled {
+                            self.listener.reply(addr, ProtoEvent::Leave(0)).await;
+                            continue;
+                        }
                         last_response.insert(addr, Instant::now());
                         match event {
                             ProtoEvent::Enter(pos) => {
@@ -195,6 +205,16 @@ impl ListenTask {
                     self.event_tx.send(event).expect("channel closed");
                 }
                 request = self.request_rx.recv() => match request.expect("channel closed") {
+                    EmulationRequest::SetEnabled(value) => {
+                        enabled = value;
+                        if !enabled {
+                            for (&addr, _) in last_response.iter() {
+                                self.emulation_proxy.remove(addr);
+                                self.listener.reply(addr, ProtoEvent::Leave(0)).await;
+                            }
+                            last_response.clear();
+                        }
+                    },
                     // reenable emulation
                     EmulationRequest::Reenable => self.emulation_proxy.reenable(),
                     // notify the other end that we hit a barrier (should release capture)
diff --git a/src/listen.rs b/src/listen.rs
index e0a9475..ce7ab81 100644
--- a/src/listen.rs
+++ b/src/listen.rs
@@ -134,8 +134,8 @@ impl LanMouseListener {
                                 let certs = dtls_conn.connection_state().await.peer_certificates;
                                 let cert = certs.first().expect("cert");
                                 let fingerprint = crypto::generate_fingerprint(cert);
-                                listen_tx.send(ListenEvent::Accept { addr, fingerprint }).expect("channel closed");
-                                spawn_local(read_loop(conns_clone.clone(), addr, conn, listen_tx.clone()));
+                                listen_tx.send(ListenEvent::Accept { addr, fingerprint: fingerprint.clone() }).expect("channel closed");
+                                spawn_local(read_loop(conns_clone.clone(), addr, conn, listen_tx.clone(), authorized_keys.clone(), fingerprint));
                             },
                             Err(e) => {
                                 if let Error::Std(ref e) = e {
@@ -250,10 +250,16 @@ async fn read_loop(
     addr: SocketAddr,
     conn: ArcConn,
     dtls_tx: Sender<ListenEvent>,
+    authorized: Arc<RwLock<HashMap<String, String>>>,
+    fingerprint: String,
 ) -> Result<(), Error> {
     let mut b = [0u8; MAX_EVENT_SIZE];
 
     while conn.recv(&mut b).await.is_ok() {
+        if !authorized.read().expect("lock").contains_key(&fingerprint) {
+            let _ = conn.close().await;
+            break;
+        }
         match b.try_into() {
             Ok(event) => dtls_tx
                 .send(ListenEvent::Msg { event, addr })
diff --git a/src/service.rs b/src/service.rs
index bf782c7..53c62cf 100644
--- a/src/service.rs
+++ b/src/service.rs
@@ -169,6 +169,8 @@ impl Service {
             Err(e) => return log::error!("error receiving request: {e}"),
         };
         match request {
+            FrontendRequest::ReleaseInput => self.capture.release(),
+            FrontendRequest::SetInputEnabled(enabled) => self.emulation.set_enabled(enabled),
             FrontendRequest::Activate(handle, active) => {
                 self.set_client_active(handle, active);
                 self.save_config();
@@ -332,6 +334,8 @@ impl Service {
 
     fn handle_capture_event(&mut self, event: ICaptureEvent) {
         match event {
+            ICaptureEvent::ControlStarted(handle) => self.notify_frontend(FrontendEvent::ControlStarted(handle)),
+            ICaptureEvent::ControlReturned => self.notify_frontend(FrontendEvent::ControlReturned),
             ICaptureEvent::CaptureBegin(handle) => {
                 // we entered the capture zone for an incoming connection
                 // => notify it that its capture should be released
